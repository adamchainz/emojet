Installation
============

Requirements
------------

Python 3.10 to 3.14 supported, including free-threaded builds.

Installation
------------

Install with **pip**:

.. code-block:: sh

    python -m pip install countryry

countryry ships binary wheels for common platforms, so installation does not normally require a Rust toolchain.
